searchState.loadedDescShard("embed_plist", 0, "Embed an <code>Info.plist</code> or <code>launchd.plist</code> file directly in your …\nEmbeds the <code>Info.plist</code> file at <code>$path</code> directly in the …\nEmbeds the <code>Info.plist</code> file in <code>&amp;[u8]</code> directly in the …\nEmbeds the <code>launchd.plist</code> file at <code>$path</code> directly in the …\nEmbeds the <code>launchd.plist</code> file in <code>&amp;[u8]</code> directly in the …\nReturns the contents of the embedded <code>Info.plist</code> file.\nReturns the contents of the embedded <code>launchd.plist</code> file.")